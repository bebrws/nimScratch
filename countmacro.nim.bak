macro countfromto(arg: untyped): untyped =
    # let nn = newNimNode(nnkStmtList)
    # let forStmt = newNimNode(nnkForStmt)
    # nn.add(forStmt)

    # return nn

    let startNum = arg[1]
    let endNum = arg[2]
        # `for` `i` `in` `1``..``3` `:`
        #     `echo` `"a"`
    return quote do:
        `echo`
        

countfromto(3..5)